name: Make PRG Docker Image CI

on:
  push:
    branches: [ update ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag make_prg:$(GITHUB_SHA)
